# ============================================================================
# TikTokAI - Complete Requirements
# ============================================================================
# Install with: pip install -r requirements.txt
# Or with bun: bun install (for Node.js dependencies in tiktok-signature)

# ============================================================================
# Core Video & Audio Processing
# ============================================================================
moviepy>=1.0.3,<2.0.0          # Video editing and manipulation
numpy>=1.21.0,<2.0.0           # Numerical operations (required by moviepy, librosa)
pillow>=9.0.0,<11.0.0          # Image processing (PIL) for video effects
imageio>=2.9.0                  # Image I/O (required by moviepy)
imageio-ffmpeg>=0.4.5          # FFmpeg wrapper for moviepy
proglog>=0.1.9                  # Progress logging (required by moviepy)

# ============================================================================
# Beat Detection & Audio Analysis (Music Video Bot)
# ============================================================================
librosa>=0.10.0,<1.0.0         # Audio analysis and beat detection
soundfile>=0.12.0               # Audio file I/O (required by librosa)
scipy>=1.9.0                    # Scientific computing (required by librosa)
resampy>=0.3.0                  # Audio resampling (required by librosa)
numba>=0.56.0                   # JIT compiler (required by librosa)

# ============================================================================
# TikTok Uploader & Browser Automation
# ============================================================================
setuptools>=65.0.0              # Required for distutils (Python 3.12+ compatibility)
fake-useragent>=1.2.0           # Generate random user agents
requests-auth-aws-sigv4>=0.6.0  # AWS Signature V4 authentication
undetected-chromedriver>=3.5.0  # Undetected Chrome driver for Selenium
selenium>=4.0.0,<5.0.0          # Browser automation

# ============================================================================
# Environment & Configuration
# ============================================================================
python-dotenv>=0.19.0,<2.0.0    # Load environment variables from .env file

# ============================================================================
# HTTP Requests & API Clients
# ============================================================================
requests>=2.28.0,<3.0.0         # HTTP library for API calls

# ============================================================================
# Task Scheduling
# ============================================================================
schedule>=1.2.0                 # Job scheduling for automated video generation

# ============================================================================
# AI/Content Generation (Optional - for lock_facts_bot.py)
# ============================================================================
openai>=1.0.0,<2.0.0            # OpenAI API client
google-generativeai>=0.3.0      # Google Gemini API client
g4f>=0.1.0                      # Free GPT API wrapper

# ============================================================================
# Video Processing & Subtitles
# ============================================================================
srt-equalizer>=0.1.0            # Subtitle equalization
assemblyai>=0.15.0              # Speech-to-text API (optional, for subtitles)

# ============================================================================
# Audio Processing
# ============================================================================
# (No audio playback dependencies - removed playsound due to compatibility issues)

# ============================================================================
# Utilities
# ============================================================================
termcolor>=2.0.0                # Colored terminal output

# ============================================================================
# Node.js Dependencies (for TikTok signature generation)
# ============================================================================
# Note: These need to be installed separately with npm/yarn/bun
# Run: cd tiktok_uploader/tiktok-signature && npm install
# Or: bun install (if using bun)
# Required packages:
#   - playwright-chromium (see tiktok_uploader/tiktok-signature/package.json)

# ============================================================================
# System Requirements
# ============================================================================
# - FFmpeg: Required for video processing
#   Install on macOS: brew install ffmpeg
#   Install on Ubuntu: sudo apt-get install ffmpeg
#   Install on Windows: Download from https://ffmpeg.org/download.html
#
# - Node.js (v14+): Required for TikTok signature generation
#   Install from: https://nodejs.org/
#
# - Chrome/Chromium: Required for browser automation
#   Usually installed automatically with undetected-chromedriver
