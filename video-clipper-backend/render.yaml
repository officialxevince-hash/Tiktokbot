services:
  - type: web
    name: video-clipper-backend
    env: node
    buildCommand: bun install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        generateValue: true

